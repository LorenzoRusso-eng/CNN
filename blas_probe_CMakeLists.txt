cmake_minimum_required(VERSION 3.20)
project(BlasProbe LANGUAGES C CXX)
find_package(BLAS)
if(BLAS_FOUND)
  message(STATUS "BLAS_FOUND=TRUE")
  message(STATUS "BLAS_LIBRARIES=${BLAS_LIBRARIES}")
else()
  message(STATUS "BLAS_FOUND=FALSE")
endif()
